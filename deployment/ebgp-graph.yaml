---
apiVersion: apps/v1
kind: Deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ebgp-graph
  template:
    metadata:
      labels:
        app: ebgp-graph
    spec:
      containers:
        - args:
            - --v
            - "5"
            - --message-server
            - "broker.jalapeno:9092"
            - --database-server
            - "http://arangodb:8529"
            - --database-name
            - "jalapeno"
          image: docker.io/iejalapeno/ebgp-graph:latest
          imagePullPolicy: Always
          name: ebgp-graph
          volumeMounts:
            - name: credentials
              mountPath: /credentials
      volumes:
        - name: credentials
          secret:
            secretName: jalapeno
metadata:
  name: ebgp-graph
  namespace: jalapeno
